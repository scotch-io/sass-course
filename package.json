{
  "name": "sass-course",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run gulp && concurrently \"npm run gulp-watch\" \"npm run lite\"",
    "lite": "lite-server",
    "gulp": "gulp",
    "gulp-watch": "gulp watch"
  },
  "devDependencies": {
    "concurrently": "^3.4.0",
    "gulp": "^3.9.1",
    "gulp-sass": "^3.1.0",
    "lite-server": "^2.3.0"
  }
}
